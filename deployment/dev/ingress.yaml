apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: gitops-demo
  labels:
    app: gitops-demo
  annotations: 
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: demo.dev.howlinkdev.com
    http:
      paths:
      - backend:
          serviceName: gitops-demo-svc
          servicePort: http

